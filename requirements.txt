# requirements.txt — MaternaCare (Streamlit Cloud deployment)
# ──────────────────────────────────────────────────────────────────────────────
# tensorflow-cpu==2.21.0 is the latest stable release (March 2026) and ships
# with a Keras 3 version new enough to load the MLP model, which was saved
# with a recent Keras 3 (uses `quantization_config` field on Dense layers).
#
# Older TF/Keras versions throw:
#   "Unrecognized keyword arguments passed to Dense: {'quantization_config': None}"
# ──────────────────────────────────────────────────────────────────────────────

numpy
pandas
scikit-learn
joblib
matplotlib
streamlit
tensorflow-cpu==2.21.0
