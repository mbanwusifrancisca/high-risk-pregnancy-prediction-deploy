# requirements.txt — MaternaCare (Streamlit Cloud deployment)
# ──────────────────────────────────────────────────────────────────────────────
# tensorflow-cpu==2.17.0 ships with Keras 3, which is required to load the
# mlp_model.keras file (it was saved with Keras 3's batch_shape format).
# Keras 2 throws "Unrecognized keyword arguments: ['batch_shape', 'optional']".
# ──────────────────────────────────────────────────────────────────────────────

numpy
pandas
scikit-learn
joblib
matplotlib
streamlit
tensorflow-cpu==2.17.0
