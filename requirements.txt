# requirements.txt — MaternaCare (Streamlit Cloud deployment)
# ──────────────────────────────────────────────────────────────────────────────
# Deployment uses Logistic Regression (best AUC-ROC = 0.933, F1 = 0.759).
# TensorFlow/Keras is no longer required since the MLP is not used at runtime.
# Build time is now ~30 seconds instead of ~5 minutes.
# ──────────────────────────────────────────────────────────────────────────────

numpy
pandas
scikit-learn
joblib
matplotlib
streamlit
